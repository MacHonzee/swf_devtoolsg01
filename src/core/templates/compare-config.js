module.exports = {
  // Used if you want to make sure that this file is properly loaded (mainly for debugging cases)
  // isUsed: () => console.log("Using app configuration"),
  //
  // Used to set uri of documentation. It has precedence over uuapp.json and package.json configurations.
  // documentation: "https://uuapp.plus4u.net/uu-bookkitg01-main/{awid}",
  //
  // Used to set default language of the uuBookKit. The tool assumes that documentation is only one language for now.
  // defaultLanguage: "en",
};
